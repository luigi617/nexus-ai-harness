# Evaluators

An `Evaluator` (protocol: `protocols/evaluator.py`) is a structured-evaluation
backend: it takes a *state* plus typed *questions* and returns typed *answers*
with calibrated probabilities.